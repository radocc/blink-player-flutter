import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:blink/app/modules/dados/dados_controller.dart';
import 'package:blink/app/app_module.dart';

void main() {
  initModule(AppModule());
  // DadosController dados;
  //
  setUp(() {
    //     dados = AppModule.to.get<DadosController>();
  });

  group('DadosController Test', () {
    //   test("First Test", () {
    //     expect(dados, isInstanceOf<DadosController>());
    //   });

    //   test("Set Value", () {
    //     expect(dados.value, equals(0));
    //     dados.increment();
    //     expect(dados.value, equals(1));
    //   });
  });
}
