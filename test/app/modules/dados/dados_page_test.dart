import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:blink/app/modules/dados/dados_page.dart';

void main() {
  testWidgets('DadosPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(DadosPage(title: 'Dados')));
    //  final titleFinder = find.text('Dados');
    //  expect(titleFinder, findsOneWidget);
  });
}
