import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('CarouselPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CarouselPage(title: 'Carousel')));
    //  final titleFinder = find.text('Carousel');
    //  expect(titleFinder, findsOneWidget);
  });
}
