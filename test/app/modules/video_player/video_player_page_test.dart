import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:blink/app/modules/video_player/video_player_page.dart';

void main() {
  testWidgets('VideoPlayerPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(VideoPlayerPage(title: 'VideoPlayer')));
    //  final titleFinder = find.text('VideoPlayer');
    //  expect(titleFinder, findsOneWidget);
  });
}
