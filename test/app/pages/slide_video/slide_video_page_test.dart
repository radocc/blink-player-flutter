import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:blink/app/pages/slide_video/slide_video_page.dart';

void main() {
  testWidgets('SlideVideoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SlideVideoPage(title: 'SlideVideo')));
    //  final titleFinder = find.text('SlideVideo');
    //  expect(titleFinder, findsOneWidget);
  });
}
