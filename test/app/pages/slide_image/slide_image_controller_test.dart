import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:blink/app/pages/slide_image/slide_image_controller.dart';
import 'package:blink/app/app_module.dart';

void main() {
  initModule(AppModule());
  // SlideImageController slideimage;
  //
  setUp(() {
    //     slideimage = AppModule.to.get<SlideImageController>();
  });

  group('SlideImageController Test', () {
    //   test("First Test", () {
    //     expect(slideimage, isInstanceOf<SlideImageController>());
    //   });

    //   test("Set Value", () {
    //     expect(slideimage.value, equals(0));
    //     slideimage.increment();
    //     expect(slideimage.value, equals(1));
    //   });
  });
}
