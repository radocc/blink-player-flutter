import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:blink/app/pages/slide_image/slide_image_page.dart';

void main() {
  testWidgets('SlideImagePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SlideImagePage(title: 'SlideImage')));
    //  final titleFinder = find.text('SlideImage');
    //  expect(titleFinder, findsOneWidget);
  });
}
